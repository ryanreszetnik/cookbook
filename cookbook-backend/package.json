{
  "name": "cookbook-backend",
  "version": "1.0.0",
  "description": "Backend of Going Out",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "serverless deploy",
    "deployScrape": "zip -r code.zip . && aws lambda update-function-code --function-name recipes-scrape-recipe --zip-file fileb://code.zip"
  },
  "author": "Ryan Reszetnik",
  "license": "ISC",
  "dependencies": {}
}
